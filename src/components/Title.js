import React from "react";

// Notes: Named export
// export const Title = props => <h2>{props.title}</h2>;

// Notes: Default export
const Title = props => <h2>{props.title}</h2>;
export default Title;
